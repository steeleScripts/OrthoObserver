import React from 'react'

const FlaggedPrayers = () => {

    /*const {
        data: users,
        isError,
        isLoading,
        isSuccess
    } = useGetFlaggedUsersQuery()

    let list = null

    if(isError) {

    }

    if(isLoading) {
        
    }

    if(isSuccess) {
        
    }
*/
    return (
        <div>FlaggedPrayers</div>
    )
}

export default FlaggedPrayers